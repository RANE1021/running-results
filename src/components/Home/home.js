import React from 'react';

const HomeComponent = () => (
  <div className="summary-styling">
    <p>The next few pages will pull results from the NYRR API and provide background info. </p>
    <p>This is an example React web application, using Bootstrap, Mobx, & Axios.</p>
  </div>
);

export default HomeComponent;
